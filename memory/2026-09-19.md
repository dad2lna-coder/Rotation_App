# 2026-09-19 — Let Them Cook Initiative Tracker Refactor

## What happened

Refactored `rotation_app/` into a new `projects/let-them-cook/` Tauri v2 app focused on **workplace improvement initiatives** instead of hard-coded NDO/Training/TSST movement tabs.

## Current state

### Completed
- New project skeleton at `projects/let-them-cook/` with modular JS
- Rust backend updated: `paths.rs`, `commands.rs`, `lib.rs`, `Cargo.toml`, `tauri.conf.json`
- Data model shifted from `{ movements: { ndo, training, tsst } }` to `{ initiatives: [{ id, name, status, owner, startDate, sections: [{ id, type, name, ideas, actions, questions, flow }] }] }`
- `examples/initiatives.json` and `examples/submit.json` created
- All Rust unit tests written (paths.rs)
- `scripts/prepare-frontend.js` and `scripts/decode-icons.js` copied unchanged
- Icons copied to `src-tauri/icons/`
- `css/style.css` copied from old project
- `index.html` rewritten for initiative list + section editor UI
- `js/data/schema.js`, `js/data/migrations.js`, `js/data/store.js`, `js/utils/strings.js`, `js/utils/tauri.js`, `js/utils/ui.js` written and syntax-checked

### In progress (not yet written)
- `js/stores/state.js` — needs rewrite to import from utils/strings, utils/tauri, utils/ui, data/store; remove duplicate function declarations
- `js/components/initiative.js` — needs rewrite: initiative list rendering, editor, add/delete initiative, add section, save initiative, render section DOM
- `js/components/section.js` — needs creation: section editor DOM with flow grid, ideas table, questions list, tasks list
- `js/init.js` — needs creation: main entry point, import all modules, boot app, attach event listeners for buttons

### Blockers
- `js/stores/` and `js/components/` directories exist but are empty (previous writes interrupted)
- `js/data/store.js` has undefined `escapeHtml` reference (not imported)
- `js/stores/state.js` has duplicate function declarations and undefined `getCurrentPayload`, `refreshFromShare`, `saveToInbox`, `collectSectionPayload`
- `index.html` references `js/init.js` as module but that file doesn't exist
- No `node_modules/` installed (need `npm install`)

## Old project (preserved, untracked)
- `rotation_app/` contains original code, all untracked in workspace
- Git branch: `Side-Quest` on `dad2lna-coder/Rotation_App`

## Key decisions
- Use vanilla JS modules (ES modules), not Svelte/Vite
- Copy-only Tauri build (no bundler)
- `data/initiatives.json` replaces `data/dashboard.json`
- Initiative/template model replaces hard-coded movement panels
- Rust path discovery reimplemented in BLADE style (no `BLADE_Alpha` dep)
# 2026-09-19 — Let Them Cook Initiative Tracker Refactor

## What happened

Refactored `rotation_app/` into a new `projects/let-them-cook/` Tauri v2 app focused on **workplace improvement initiatives** instead of hard-coded NDO/Training/TSST movement tabs.

## Current state (end of session)

### Completed
- New project skeleton at `projects/let-them-cook/` with modular JS
- Rust backend updated: `paths.rs`, `commands.rs`, `lib.rs`, `Cargo.toml`, `tauri.conf.json`
- Data model shifted from `{ movements: { ndo, training, tsst } }` to `{ initiatives: [{ id, name, status, owner, startDate, sections: [{ id, type, name, ideas, actions, questions, flow }] }] }`
- `examples/initiatives.json` and `examples/submit.json` created
- All Rust unit tests written (paths.rs)
- `scripts/prepare-frontend.js` and `scripts/decode-icons.js` copied unchanged
- Icons copied to `src-tauri/icons/`
- `css/style.css` copied from old project
- `index.html` rewritten for initiative list + section editor UI
- All JS modules written and syntax-checked:
  - `js/stores/state.js` — state management, payload builder, metrics, refresh/save
  - `js/data/schema.js` — SCHEMA_VERSION, EMPTY_PAYLOAD, isValidPayload
  - `js/data/migrations.js` — migrateToV3 (old movements→new initiatives)
  - `js/data/store.js` — collect/apply for ideas, tasks, questions, flow, problems
  - `js/utils/strings.js` — escapeHtml, normalizeText
  - `js/utils/tauri.js` — isTauri, invokeCommand
  - `js/utils/ui.js` — showToast, toggleMoreActions
  - `js/components/initiative.js` — initiative list, editor, CRUD
  - `js/components/section.js` — section editor DOM with flow grid, ideas, questions, tasks
  - `js/init.js` — main entry, boots app, attaches event listeners

### Still needs (low priority)
- `store.js` calls `getCurrentPayload()` — exported from state.js under different path? Need to verify import works
- `init.js` imports `importJsonPayload` from `js/actions/data.js` which doesn't exist yet
- `index.html` needs `id="core-problems-list"` added to the problem list container for `applyProblems()`
- `npm install` hasn't been run; `node_modules/` is empty
- No `cargo test` or `npx tauri build` verification run yet

### Blocker notes from mid-session
- `js/stores/` and `js/components/` directories existed but were empty after interrupted writes
- `js/stores/state.js` had duplicate function declarations (exported wrapper with same name as imported function)
- Fixed by removing re-exports that shadowed imports and adding dedicated exported helpers

## Old project (preserved, untracked)
- `rotation_app/` contains original code, all untracked in workspace
- Git branch: `Side-Quest` on `dad2lna-coder/Rotation_App`

## Key decisions
- Use vanilla JS modules (ES modules), not Svelte/Vite
- Copy-only Tauri build (no bundler)
- `data/initiatives.json` replaces `data/dashboard.json`
- Initiative/template model replaces hard-coded movement panels
- Rust path discovery reimplemented in BLADE style (no `BLADE_Alpha` dep)
- Product name: **Let Them Cook — Workplace Improvement Initiative Tracker**
- `projects/let-them-cook/` replaces `rotation_app/` for active development
